name: Daily Commit

on:
  schedule:
    # Exécution quotidienne à 12:00 UTC (14:00 CET en hiver / 13:00 CET en été)
    - cron: '0 12 * * *'
  workflow_dispatch: # Permet d'exécuter manuellement

# Donner les permissions d'écriture au GITHUB_TOKEN
permissions:
  contents: write

jobs:
  daily-commit:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      with:
        # Token avec permissions de push
        token: ${{ secrets.GITHUB_TOKEN }}
        
    - name: Setup Git config
      run: |
        git config --local user.email "action@github.com"
        git config --local user.name "GitHub Action"
        
    - name: Create daily log entry
      run: |
        echo "$(date '+%Y-%m-%d %H:%M:%S UTC') - Daily commit" >> daily-log.txt
        
    - name: Commit changes
      run: |
        git add daily-log.txt
        if git diff --staged --quiet; then
          echo "Aucun changement à committer"
        else
          # Déterminer si c'est le week-end
          DAY_OF_WEEK=$(date '+%u')  # 1=Lundi, 5=Vendredi, 6=Samedi, 7=Dimanche
          HOUR=$(date '+%H')
          
          if [ "$DAY_OF_WEEK" -eq 5 ] && [ "$HOUR" -ge 12 ]; then
            # Vendredi après 12h
            WEEKEND_STATUS="🎉 OUI"
          elif [ "$DAY_OF_WEEK" -eq 6 ] || [ "$DAY_OF_WEEK" -eq 7 ]; then
            # Samedi ou dimanche
            WEEKEND_STATUS="🎉 OUI"
          else
            # Autres cas
            WEEKEND_STATUS="⏰ PAS ENCORE"
          fi
          
          git commit -m "📅 $(date '+%Y-%m-%d') - C'est bientôt le week-end ? $WEEKEND_STATUS"
          git push
        fi